# auto_chess 阶段三前补全说明

## 本次修改目标

本次修改围绕 PA 文档中阶段三之前的要求展开，重点补齐准备、战斗、结算循环，增强敌人回合生成，修复升星和羁绊的明显错误，并把核心函数注释整理到能读懂模块职责的程度。

## 主要完成内容

1. 正式开局流程

`MainWindow` 构造函数不再自动放置测试英雄和测试敌人。玩家需要从商店购买英雄、拖到棋盘后，按空格开始战斗。

2. 三阶段状态流

`GameManager` 现在明确使用：

- `Preparation`：购买、刷新、拖拽布阵。
- `Battle`：单位自动寻敌、移动、攻击、施法。
- `Settlement`：展示胜负结果，发放奖励，然后自动回到准备阶段。

3. 敌人随轮次增强

`spawnEnemyRound(int round)` 会根据轮次提高敌人数量、生命和攻击。后续轮次还会混入 `Garen`、`Ryze` 这样的敌方英雄，使战斗不再只是固定小怪。

4. 战斗移动优化

`Unit::handleMoving` 从简单贪心移动改为 BFS 搜索。单位会在可达格子中选择更接近目标的位置，减少被其他单位挡住后原地卡住的问题。

5. 胜负与死亡清理

`cleanupDeadUnits` 现在独立清理所有阵亡单位，不再依赖装备掉落分支。敌方死亡时仍保留概率掉落装备。`checkBattleResult` 会清理敌方残留单位，重置玩家存活单位，为下一轮做准备。

6. 升星和羁绊修复

`Unit` 默认星级从 `3` 改为 `1`，三合一升星逻辑才能正常从 1 星开始。`Garen` 的羁绊从 `Warrior` 改为 `Vanguard`，与 `GameManager` 中的羁绊统计和 buff 判断一致。

7. 人口初始值调整

`Player` 初始等级从 5 改为 3，因此开局人口上限为 3，更符合 PA 文档建议的初始人口范围。

8. 头文件包含修复

`unit.h` 中的 `#include<Item.h>` 改为 `#include "Item.h"`，使项目内头文件按本地文件查找，减少不同构建环境下找不到头文件的问题。

9. UI 底部布局优化

把窗口高度扩大到 830，并把底部区域改成三层：

- 备战区：`BENCH_Y = 550`
- 装备库存：`ITEM_GRID_Y = 635`
- 商店：`SHOP_Y = 730`

这样装备栏不会再和备战区重叠，商店也和上方区域留出间隔。

10. 存档与读档

新增 `GameManager::saveGame` 和 `GameManager::loadGame`。存档写入 `autosave.json`，保存内容包括：

- 当前阶段、关卡轮数、玩家血量
- 玩家金币、等级、经验
- 棋盘单位和备战区单位
- 单位的生命、攻击、射程、法力、星级、羁绊
- 单位已穿戴装备
- 装备库存
- 商店当前卡牌

UI 右侧新增“保存存档 (S)”和“读取存档 (L)”按钮，也可以直接按 `S` 保存、按 `L` 读取。

## 涉及文件

- `gamemanager.cpp`：重写为清晰注释版本，补齐阶段流、敌人增强、结算、死亡清理、商店、升星、羁绊统计。
- `unit.cpp`：重写为清晰注释版本，补齐状态机说明，并加入 BFS 移动。
- `mainwindow.cpp`：移除开局测试单位和测试敌人，新增保存/读取按钮与快捷键处理。
- `mainwindow.h`：整理窗口布局常量，重新排布备战区、装备栏和商店。
- `unit.h`：默认星级修正为 1，本地包含 `Item.h`。
- `heroes.h`：修正 Garen 羁绊名为 `Vanguard`。
- `player.cpp` / `player.h`：初始等级/人口改为 3，并增加读档恢复玩家资源的接口。

## 目前注意事项

现有 GUI 里仍有部分中文字符串是乱码，这主要影响显示观感，不影响本次补齐的核心战斗逻辑。后续可以集中把 UI 文案统一改成 UTF-8 中文。

当前命令行增量编译时，`gamemanager.cpp` 已进入编译，但 `main.cpp` 触发 Qt 6.9.3 头文件内部的 `static_assert`，错误位置在 Qt 安装目录的 `qcomparehelpers.h/qmetatype.h/qvariant.h`，不是业务源码里的具体语法错误。建议继续用 Qt Creator 的原 Kit 构建；如果仍复现，需要检查 Qt/MinGW 版本匹配或重新生成构建目录。
